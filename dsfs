* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
    background-color: #f9fafc;
    color: #1e293b;
    line-height: 1.6;
}

/* Шапка */
.custom-button {
    display: inline-block;
    margin-left: 40cm;
    padding: 12px 32px;
    background: #183675;
    color: rgb(255, 255, 255);
    justify-content: space-between;
    align-items: left;
    min-height: 1vh;
    font-size: 18px;
    font-weight: 600;
    border-radius: 50px;
    text-decoration:  none;
    transition: all 0,3 ease;
    border: none;
    cursor: pointer;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}
.custom-button:hover{
    background: #004cff;
    transform: translate(-2px);
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
}
.custom-button:active {
    transform: translate(1px);
}
.header {
    background: linear-gradient(135deg, #004cff, #0062ff);
    color: white;
    padding: 2rem 0;
    text-align: center;
    box-shadow: 0 4px 10px rgba(0,0,0,0.2);
}

.header h1 {
    font-size: 2.8rem;
    letter-spacing: 1px;
    margin-bottom: 0.5rem;
}

.header p {
    font-size: 1.2rem;
    opacity: 0.9;
}

/* Навигация */
.navbar {
    background-color: #183675;
    padding: 1rem 0;
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: 0 2px 8px rgba(255, 36, 36, 0.171);
}

.navbar ul {
    list-style: none;
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 2rem;
}

.navbar a {
    color: #cbd5e1;
    text-decoration: none;
    font-weight: 500;
    font-size: 1.1rem;
    transition: 0.3s;
    padding: 0.5rem 0;
    border-bottom: 2px solid transparent;
}

.navbar a:hover {
    color: white;
    border-bottom-color: #3b82f6;
}

/* Основной контейнер */
.container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 2rem 1.5rem;
}

/* Разделы */
.section {
    margin-bottom: 4rem;
    scroll-margin-top: 70px; /* для плавной навигации */
}

.section-title {
    font-size: 2rem;
    margin-bottom: 2rem;
    color: #000000;
    position: relative;
    padding-bottom: 0.75rem;
}

.section-title::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: 0;
    width: 80px;
    height: 4px;
    background: #3b82f6;
    border-radius: 2px;
}

/* Портфолио */
.portfolio-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.8rem;
}

.portfolio-item {
    background: white;
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 5px 12px rgba(0,0,0,0.03);
    transition: 0.3s;
    border: 1px solid #edf2f7;
}

.portfolio-item:hover {
    box-shadow: 0 12px 24px rgba(0,0,0,0.08);
    transform: scale(1.02);
}

.portfolio-img {
    height: 180px;
    background-color: #d9e2ef;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #3b5a8c;
    font-weight: 500;
    background-image: linear-gradient(45deg, #cbd5e1 25%, #e9edf2 25%);
    background-size: 30px 30px;
}

.portfolio-info {
    padding: 1.2rem;
}

.portfolio-info h4 {
    margin-bottom: 0.5rem;
    color: #0f172a;
}

.portfolio-info p {
    color: #55657b;
    font-size: 0.9rem;
}

/* Подвал */
.footer {
    background-color: #0953ff;
    color: #94a3b8;
    text-align: center;
    padding: 2rem 0;
    margin-top: 3rem;
}

.footer p {
    margin-bottom: 0.5rem;
}

.social-links {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin: 1rem 0;
}

.social-links a {
    color: #cbd5e1;
    font-size: 1.4rem;
    transition: 0.3s;
}

.social-links a:hover {
    color: #3b82f6;
    transform: translateY(-3px);
}

/* Адаптив */
@media (max-width: 600px) {
    .header h1 {
        font-size: 2.2rem;
    }

    .navbar ul {
        gap: 1rem;
    }

    .navbar a {
        font-size: 0.95rem;
    }

    .hero-text h2 {
        font-size: 1.8rem;
    }

    .section-title {
        font-size: 1.8rem;
    }

    .contact-flex {
        padding: 1.5rem;
    }
}
